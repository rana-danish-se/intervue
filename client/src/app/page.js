import Home from "@/pages/Home";

export default function Page() {
  return <Home />;
}

/**
 * Role: Root Landing Page Route
 * What it has: 1 pure UI component
 * What it is doing: The `Page` function strictly acts as a Next.js server routing module that returns and renders the primary `Home` landing UI.
 * Where it is being used: Automatically loaded by Next.js when navigating to the absolute root (`/`).
 */
